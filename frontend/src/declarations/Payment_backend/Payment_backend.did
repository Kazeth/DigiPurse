type Result_1 = 
 variant {
   err: text;
   ok: nat;
 };
type Result = 
 variant {
   err: text;
   ok: text;
 };
type PaymentManager = 
 service {
   getMyBalance: (canisterIdText: text) -> (nat);
   getMyPrincipal: () -> (principal) query;
   sendTokens: (canisterIdText: text, to_principal: principal, amount:
    nat) -> (Result_1);
   topUpCanister: (targetCanisterId: principal, icpAmount: nat) -> (Result);
 };
service : () -> PaymentManager
